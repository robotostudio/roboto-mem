# Enters the offline demo world built by scripts/make-demo-world.sh.
# Hidden: viewers open on a clean ❯ prompt already inside the bound project.
Hide
Type "source /tmp/roboto-mem-demo/env.zsh && clear"
Enter
Sleep 1s
Show
Sleep 500ms
